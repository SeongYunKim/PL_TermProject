mainprog error1;

function plus(x: int; y: int): int;
    int a, b, c;
	begin
		return x+y
	end

function min(x: int; y: int): int;
    int a;
	begin
		return x-y
	end

begin
    print(plus(1,3))
end

