mainprog error1;

function plus(x: int; y: int): int;
    int a, b, c;
	begin
		return x+y
	end

function min(x: int; y: int): int;
    int a, x;
	begin
	    a = z;
		return x-a
	end

begin
    plus(a, 4)
end

